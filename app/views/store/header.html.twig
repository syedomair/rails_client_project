
<nav class="navbar" >
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('app_index')}}">
                <img src="{{asset('bundles/app/images/logo.jpg')}}" alt="Store Logo" height="60" width="60">
            </a>
            <a class="navbar-brand" href="{{ path('app_index')}}">
                Symfony Sample API Client Application
            </a>
        </div><!-- End of navbar header -->
        <div class="collapse navbar-collapse" >
            <ul class="nav navbar-nav navbar-right">
                <li class="active">
                    <a href="{{ path('app_index')}}">Home</a>
                </li>       	
                {% if not  app.session.has('username') %}
                    <li class="">
                        <a href="#modal-sign-in" class="" data-toggle="modal" data-target="#modal-sign-in">Sign In</a>
                    </li>       	
                    <li class="">
                        <a href="#modal-register" class="" data-toggle="modal" data-target="#modal-register">Register</a>
                    </li>       	
                {%else%}
                    <li class="">
                        <a href="{{path('app_sign_out')}}" class="">Sign Out</a>
                    </li>       	
                {%endif%}

            </ul>
        </div><!-- End of nav-collapse -->                
    </div><!-- End of container class -->
</nav><!-- End of navigation -->





